{
  "name": "net-worth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --watchAll",
    "test-ci": "jest",
    "coverage-watch": "jest --watchAll --config jest.config.json",
    "coverage": "jest --coverage --config jest.config.json",
    "coveralls": "jest --coverage --config jest.config.json && cat ./coverage/lcov.info | coveralls",
    "format": "prettier --write \"{src,tests}/**/*.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "degiro": "git+https://github.com/kraag22/degiro.git",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "jest": "^28.0.2",
    "jsdom": "^19.0.0",
    "mustache-express": "^1.3.2",
    "puppeteer": "^13.7.0",
    "sqlite3": "^5.0.6",
    "winston": "^3.7.2"
  },
  "devDependencies": {
    "coveralls": "^3.1.1",
    "prettier": "^2.6.2"
  }
}
